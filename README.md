# TODO: Добавить поддержку подключения через main_endpoint.py, stream_endpoint и websocket_endpoint для модели yolo (triton_api) для трекера.

# TODO: Добавить поддержку подключения через main_endpoint.py, stream_endpoint и websocket_endpoint для модели donut (triton_api), 
# с помощью которой будет распознваться текст.
# 1. Вызываем генератор-трекер, чтобы получить трекированные рамки с текстом.
# 2. Собираем несколько кадров одного текста в пачку, с конкретным ReID.
# 3. Агрегируем кадры (сейчас трудно в реализации, просто распознаем один из кадров с помощью donut модели).
# 4. Получаем через генератор-распознаватор распознанный текст на видео.
# 5. Используем распознаватор в новом main ендпоинте в server, добавляя логику сохранения нужных данных в бд.

# TODO: Реализовать ендпоинт для обработки видео.
# 1. Скачивание видео рутуба по ссылке и сохранение в tmp.
# 2. Добавляем метаданные в бд.
# 3. Используем генератор-распознаватель, чтобы в стриме получать распознанный текст.
# 4. Отправляем в реальном времени по этому ендпоинту (сокет) распознанный текст.
# 5. После окончания обработки/при ошибке/при остановке добавить собранные данные (мы их ппри получении с генератора кешируем) в бд.
# 6. Прислать ответ на фронт, что мы закончили (исходя из ошибки/успешности и тд)

# TODO: На тритон сервере нужно добавить поддержку помимо UploadFile -> изображения в виде numpy.

# TODO: Реализовать такой же ендпоинт для публичного использования на app_public, но с ограничениями на использование.

# TODO: Провести тестирование ендпоинтов.

# TODO: Написать DockerFile, build.sh, run.sh для запуска через контейнер.

# TODO: Сука, еще фронт делать :(